name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Cache cargo registry
        uses: actions/cache@v1
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.toml') }}

      - name: Cache cargo index
        uses: actions/cache@v1
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.toml') }}

      - name: Cache cargo build
        uses: actions/cache@v1
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.toml') }}

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Run Cargo tests
        run: cargo test --verbose

      - name: Run wasm integration tests day01
        run: wasm-pack test --firefox --headless
        working-directory: ./day01

      - name: Run wasm integration tests day02
        run: wasm-pack test --firefox --headless
        working-directory: ./day02

      - run: wasm-pack build day01
        working-directory: ./day01

      - run: wasm-pack build day02
        working-directory: ./day02

      - run: npm install
        working-directory: ./www

      - run: npm run build
        working-directory: ./www

      - name: Deploy to Netlify
        uses: netlify/actions/cli@master
        with:
          args: >
            deploy --dir www/dist --prod
            --auth ${{ secrets.NETLIFY_AUTH_TOKEN }} --site ${{ secrets.NETLIFY_SITE_ID }}
            --message DeployGithubActions
        if: github.ref == 'refs/heads/master'
